import { styles } from '../utils/styles';

const Line = () => {
  return (
    <div className={`h-16 ${styles.flexCenter}`}>
      <hr className='w-[96.5%] border-[1.4px] mx-auto' />
    </div>
  );
};

export default Line;
